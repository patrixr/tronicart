import { createPixelEditor, editPixels } from './pixels';
import { Palette } from './palette';
import {
    colorsEqual,
    lerpingColor,
    copyColor,
    isColor
} from './colors';

export {
    Palette,
    colorsEqual,
    createPixelEditor,
    editPixels,
    lerpingColor,
    copyColor,
    isColor
}